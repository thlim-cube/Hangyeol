# 벤치마크

`swift run -c release HangyeolBenchmark`로 측정했다.

| 항목 | 값 |
| --- | --- |
| Hangyeol | v2.7.4 stable |
| 빌드 | Release |
| 배포 타깃 | macOS 14.0+ (`LC_BUILD_VERSION minos 14.0`) |
| 측정 환경 | Apple M5 Pro, 48GB RAM |
| OS | macOS 26.5 (25F71) |
| Swift | Apple Swift 6.3.2 |
| 벤치마크 소스 | `Sources/HangyeolBenchmark/main.swift` |

## 요약

| 항목 | 결과 |
| --- | ---: |
| 한자 사전 최초 로딩 | 1002.48ms |
| 한자 검색 20키 (Cold) | 0.20ms |
| 한자 검색 20키 (Cached) | 0.00ms |
| 한자 검색 버스트 10,000회 | 0.25μs/op |
| 자모 검색 버스트 10,000회 | 0.51μs/op |
| 초성 자모 변환+검색 버스트 10,000회 | 0.46μs/op |
| 동시 검색 | 40,000회, 에러 0건 |
| 커서 좌표 검증 | 9개 케이스 통과 |
| `isValidCursorRect` 버스트 100,000회 | 0.11μs/op |
| 최종 메모리 증가 | +74.8MB |

벤치마크 종합 결과는 `ALL TESTS PASSED`다.

## 한자 검색

`HanjaManager.search(key:)`의 성능이다. `hanja.txt`를 libhangul의 `HanjaTable`에 적재한 뒤 검색하며, 검색 결과는 LRU 캐시(최대 32개)에 저장한다.

| 항목 | 수치 | 설명 |
| --- | ---: | --- |
| 사전 로딩 (Cold) | 1002.48ms | `hanja.txt` 파싱 및 테이블 구축. 최초 한자키 입력 시 1회 발생 |
| 검색 20키 (Cold) | 0.20ms | 캐시가 비어 있는 상태에서 20개 키 검색 |
| 검색 20키 (Cached) | 0.00ms | 캐시 히트 경로 |
| 검색 버스트 10,000회 | 0.25μs/op | 단일 키 반복 검색. 캐시 히트 최대 처리량 |
| 캐시 미스 순환 (40키 × 100회) | 1.59ms | LRU 캐시 크기(32)를 초과하는 키 순환 |

검색 결과 수 참고:

| 키 | 결과 수 | 키 | 결과 수 |
| --- | ---: | --- | ---: |
| 가 | 125 | 바 | 0 |
| 나 | 45 | 사 | 299 |
| 다 | 13 | 아 | 108 |
| 라 | 73 | 자 | 191 |
| 마 | 76 | 차 | 112 |

## 자모 특수문자 검색

자음 입력 후 한자키를 누르면 특수문자 후보를 표시한다. `jamo_symbols.json`은 14개 자음, 총 390개 특수문자를 담고 있다.

libhangul은 preedit 문자를 초성 자모(Choseong Jamo, U+1100~U+1112)로 반환할 수 있고, JSON 키는 호환 자모(Compatibility Jamo, U+3131~U+314E)를 사용한다. `HanjaManager`는 초성 자모를 호환 자모로 변환한 뒤 검색한다.

| 항목 | 수치 | 설명 |
| --- | ---: | --- |
| JSON 최초 로딩 포함 검색 | 1.51ms | 최초 자모 한자키 입력 시 1회 |
| 전체 14키 검색 | 0.03ms | 14개 자음 전체 순회 |
| 버스트 10,000회 (ㅁ) | 0.51μs/op | 호환 자모 직접 검색 |
| 초성 변환+검색 14키 | 0.06ms | U+1100 계열 초성 자모 변환 포함 |
| 초성 변환+검색 10,000회 (ᄆ→ㅁ) | 0.46μs/op | 변환 포함 캐시 경로 |

자모별 특수문자 수:

| 자음 | 수 | 자음 | 수 | 자음 | 수 | 자음 | 수 |
| --- | ---: | --- | ---: | --- | ---: | --- | ---: |
| ㄱ | 32 | ㄹ | 21 | ㅅ | 15 | ㅊ | 14 |
| ㄴ | 18 | ㅁ | 58 | ㅇ | 15 | ㅋ | 14 |
| ㄷ | 42 | ㅂ | 28 | ㅈ | 20 | ㅌ | 48 |
|  |  |  |  |  |  | ㅍ | 29 |
|  |  |  |  |  |  | ㅎ | 36 |

14개 호환 자모와 14개 초성 자모의 결과 수가 모두 일치했다.

## 동시성

`HanjaManager` 검색 캐시(`searchCache`, `cacheOrder`)의 thread safety를 검증한다.

| 항목 | 수치 | 설명 |
| --- | ---: | --- |
| 순차 5,000회 | 13.90ms, 에러 0건 | 한자+자모+초성 키 혼합 검색 |
| 동시 5,000회 × 8스레드 | 92.48ms, 에러 0건 | 총 40,000회 동시 검색 |

캐시는 `NSLock`으로 보호된다. 실제 IMK 입력 경로는 주로 메인 스레드에서 호출되지만, 방어적으로 동시 접근을 검증한다.

## 커서 좌표 검증

`HangulComposer.isValidCursorRect()`는 한자 후보창 위치 결정 시 앱이 반환한 좌표가 유효한지 판별한다. Chromium 계열 앱은 `firstRect(forCharacterRange:)`에서 쓰레기값이나 영점 좌표를 반환할 수 있어 이를 걸러낸다.

| 입력 | 기대 | 결과 | 설명 |
| --- | --- | --- | --- |
| (607, 637, w=1, h=19) | valid | pass | 정상 커서 좌표 |
| (458, 978, w=0, h=18) | valid | pass | Accessibility API fallback. width 0 허용 |
| (100, 300, w=10, h=20) | valid | pass | 일반 좌표 |
| (0, 0, w=0, h=0) | invalid | pass | Chromium 좌표 조회 실패 |
| (1.6e-314, 95886, ..., h=-1) | invalid | pass | 초기화되지 않은 메모리값 |
| (-100, 500, w=10, h=20) | invalid | pass | 음수 x 좌표 |
| (500, 500, w=10, h=-5) | invalid | pass | 음수 height |
| (100000, 500, w=10, h=20) | invalid | pass | 비현실적 x |
| (500, 100000, w=10, h=20) | invalid | pass | 비현실적 y |

9개 케이스 전체 통과. 버스트 100,000회에서 0.11μs/op.

## HangulComposer

| 항목 | 수치 | 설명 |
| --- | ---: | --- |
| 인스턴스 생성 | 0.11ms | libhangul 컨텍스트 초기화 포함 |
| 입력모드 전환 10,000회 | 0.01μs/op | `setInputMode(composer.inputMode.toggled)` 호출. 상태 바 업데이트 제외 |

## 메모리

| 단계 | 사용량 | 증가 |
| --- | ---: | ---: |
| 프로세스 시작 | 9.3MB | - |
| 한자 사전 로딩 후 | 78.0MB | +68.6MB |
| 자모 테이블 추가 후 | 78.5MB | +0.5MB |
| 벤치마크 종료 후 | 84.2MB | +74.8MB |

한자 사전 테이블이 메모리 증가의 대부분을 차지한다. 자모 특수문자 테이블의 추가 비용은 0.5MB 수준이다.

## 검증 상태

벤치마크 실행 중 다음 항목이 모두 통과했다.

- 자모 매핑 일치
- 순차 검색 안전성
- 동시 검색 안전성
- 커서 좌표 유효성 판별
- HangyeolBenchmark 실행 파일의 macOS 최소 버전 `14.0` 확인
